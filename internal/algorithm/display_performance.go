package algorithm

import (
	"fmt"

	"github.com/Melih7342/huffman-file-compression/internal/models"
)

func Performance(result models.JobResult) {

	sizeReduction := result.OriginalSize - result.NewSize
	if err != nil {
		fmt.Println("Error calculating size reduction")
	}
}
